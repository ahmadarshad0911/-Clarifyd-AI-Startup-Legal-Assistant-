[pytest]
asyncio_mode = auto
markers =
    benchmark: live ML-style benchmarks (skipped by default; run with -m benchmark)
# Skip benchmark-marked tests in the default run so unit tests stay fast.
addopts = -m "not benchmark"
